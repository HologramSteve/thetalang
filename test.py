import string

chars = list(string.ascii_lowercase)

for char in chars:
    data = ""
    for bit in list(input("> ")):
        data = data + f"write b to 240\n"
        data = data + f"write c to 241"
        